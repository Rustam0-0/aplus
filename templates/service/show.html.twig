{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ service.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ service.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ service.price }}</td>
            </tr>
            <tr>
                <th>Date_add</th>
                <td>{{ service.dateAdd ? service.dateAdd|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_update</th>
                <td>{{ service.dateUpdate ? service.dateUpdate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                {# <td>{{ service.picture }}</td> #}
                <td><img class="img-fluid" width="100" src="/assets/images/SERVICE/{{ service.picture }}"></td>
            </tr>
            <tr>
                <th>Picture2</th>
                {# <td>{{ service.picture2 }}</td> #}
                <td><img class="img-fluid" width="100" src="/assets/images/SERVICE/{{ service.picture2 }}"></td>
            </tr>
            <tr>
                <th>Picture3</th>
                {# <td>{{ service.picture3 }}</td> #}
                <td><img class="img-fluid" width="100" src="/assets/images/SERVICE/{{ service.picture3 }}"></td>
            </tr>
            <tr>
                <th>Picture4</th>
                {# <td>{{ service.picture4 }}</td> #}
                <td><img class="img-fluid" width="100" src="/assets/images/SERVICE/{{ service.picture4 }}"></td>
            </tr>
            <tr>
                <th>Picture5</th>
                {# <td>{{ service.picture5 }}</td> #}
                <td><img class="img-fluid" width="100" src="/assets/images/SERVICE/{{ service.picture5 }}"></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_index') }}">back to list</a>

    <a href="{{ path('app_service_edit', {'id': service.id}) }}">edit</a>

    {{ include('service/_delete_form.html.twig') }}
{% endblock %}
